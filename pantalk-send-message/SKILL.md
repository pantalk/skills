---
name: pantalk-send-message
description: Send a message to a chat channel or thread via any connected platform (Slack, Discord, Mattermost, Telegram, WhatsApp, Twilio).
---

Use this skill when you need to send a message to a specific channel, thread, or user on any chat platform connected through pantalkd.

## Prerequisites

- `pantalkd` must be running with at least one bot configured.
- The target bot must be defined in your pantalk config.

## Send a Message

```bash
pantalk send --bot <bot-name> --channel <channel-id> --text "Your message here"
```

The daemon automatically resolves the platform from the bot's `type` - no need to specify it.

### Required Flags

| Flag                                    | Description                                |
| --------------------------------------- | ------------------------------------------ |
| `--bot`                                 | Bot name as defined in your pantalk config |
| `--text`                                | The message content to send (use `-` to read from stdin) |
| `--channel` or `--target` or `--thread` | At least one destination identifier        |

### Reply to a Thread

```bash
pantalk send --bot ops-bot --channel C0123ABCD --thread 1700000000.000001 --text "Acknowledged, investigating now."
```

### Send to a Generic Target

```bash
pantalk send --bot support-bot --target 1234567890 --text "Ticket resolved. Closing thread."
```

## Send a File Attachment

Attach one or more local files with `--attach`. `--text` becomes the caption and may be omitted when attaching:

```bash
# Send an image (rendered inline on platforms that support it)
pantalk send --bot ops-bot --channel C0123ABCD --text "Latency graph for the last hour" --attach ./latency.png

# Send multiple files in one command
pantalk send --bot ops-bot --channel C0123ABCD --attach ./report.pdf --attach ./raw-data.csv
```

Paths are resolved relative to your current directory before being handed to the daemon. Attachment support is currently available on **Telegram**; other platforms will report an error.

Attachable files are allowlist-gated by the daemon: the file must live under one of the directories listed in `server.media.attach_roots` in the pantalk config. If the send fails with `outbound attachments are disabled` or `outside the configured attach_roots`, ask the operator to add the directory to that list - do not try to work around the restriction by copying the file elsewhere.

## Reading Message Text from Stdin

For multi-line messages or when content is generated by another command, pass `--text -` to read from stdin:

```bash
# Pipe generated content directly
echo "Deployment complete: $(git log --oneline -1)" | pantalk send --bot ops-bot --channel C0123ABCD --text -

# Send a file's contents as a message
cat report.txt | pantalk send --bot ops-bot --channel C0123ABCD --text -

# Compose a multi-line message
printf "Build summary:\n- Tests: passed\n- Coverage: 94%%\n" | pantalk send --bot ci-bot --channel C0123ABCD --text -
```

When `--text` is omitted entirely and stdin is not a terminal (e.g. when called as part of a pipeline), message text is read from stdin automatically:

```bash
# Implicit stdin when in a pipeline
generate-report | pantalk send --bot ops-bot --channel C0123ABCD
```

## JSON Output

JSON output is automatic when stdout is not a terminal (e.g. when called by an agent). You can also force it:

```bash
pantalk send --bot ops-bot --channel C0123ABCD --text "Update deployed" --json
```

## When to Use

- Posting status updates to an ops channel
- Replying to a user question in a support thread
- Sending alerts or notifications across platforms
- Confirming task completion to a requesting user
- Forwarding LLM-generated content to a chat channel
